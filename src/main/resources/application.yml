spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/flightdb
    username: flightuser
    password: flightpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration